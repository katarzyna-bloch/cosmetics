import styled from 'styled-components'

const Main = styled.main`
  position: relative;
  display: flex;
  justify-content: center;
  flex: 1;
  padding-bottom: 15px;
`

export default Main
